Using NodeJS/Yargs to organize AWS deployment logic.

Do `npm i` to install dependencies before using it.

* `./tool deploy` to deploy.
* `./tool test` to test.
* `./tool outputs` to show stack outputs.
* `./tool undeploy` to undeploy.
