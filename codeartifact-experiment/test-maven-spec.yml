version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - echo "127.0.0.1 repo1.maven.org" >> /etc/hosts
      - cd codeartifact-experiment/dummy-java-app
      - ./gradlew test
